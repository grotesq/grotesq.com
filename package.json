{
  "private": true,
  "scripts": {
    "clean": "rimraf .next",
    "build": "yarn clean && yarn build.next",
    "build.next": "next build",
    "start": "next start -p $PORT",
    "dev": "next dev",
    "deploy": "yarn build && up",
    "deploy:production": "yarn build && up production",
    "deploy:root": "yarn build && up root",
    "deploy:team": "yarn build && up team",
    "deploy:all": "yarn deploy:production; yarn deploy:root; yarn deploy:team"
  },
  "devDependencies": {
    "cpy-cli": "^3.1.0",
    "move-cli": "^1.2.1",
    "prettier": "1.19.1",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "gsap": "^3.1.1",
    "next": "^9.2.1",
    "next-seo": "^4.1.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  }
}
