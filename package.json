{
  "private": true,
  "scripts": {
    "build": "yarn build.next && yarn export",
    "build.next": "next build",
    "export": "next export",
    "start": "next start",
    "dev": "next dev",
    "deploy": "yarn build && aws s3 sync ./out/ s3://grotesq-official/ --delete"
  },
  "devDependencies": {
    "cpy-cli": "^3.1.0",
    "move-cli": "^1.2.1",
    "prettier": "1.19.1",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "gsap": "^3.1.1",
    "next": "^9.2.1",
    "next-seo": "^4.1.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  }
}
